import type { ThemeConstants } from "@/types/theme"

const themeConstants: ThemeConstants = {
  BASE_FONT_SIZE_PX: 20,
  HEADING_SIZE_RATIO: 4 / 3,
  MAX_SENTENCE_LENGTH_CH: 65,
  TOUCH_TARGET_SIZE_PX: 44,
}

export default themeConstants
